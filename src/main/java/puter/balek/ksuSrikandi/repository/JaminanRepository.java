package puter.balek.ksuSrikandi.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import puter.balek.ksuSrikandi.model.JaminanModel;

public interface JaminanRepository extends JpaRepository<JaminanModel, Long> {
    // Please define method if needed. Empty is fine too. You can still do basic CRUD

}
